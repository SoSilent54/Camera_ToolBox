name: Release

"on":
  push:
    tags:
      - "**"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag, for example v0.1.0"
        required: true
        type: string

permissions:
  contents: read

env:
  RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}

jobs:
  build-native:
    name: Build ${{ matrix.platform }} ${{ matrix.environment }}
    runs-on: ${{ matrix.runner }}

    strategy:
      fail-fast: false
      matrix:
        platform:
          - local
          - cv610
          - ssh
        environment:
          - macos-aarch64
          - windows-x86_64
        include:
          - environment: macos-aarch64
            runner: macos-14
          - environment: windows-x86_64
            runner: windows-latest

    steps:
      - name: Check out source
        uses: actions/checkout@v4
      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Build selected release
        shell: bash
        run: ./build.sh "${{ matrix.platform }}" release

      - name: Package Unix release
        if: runner.os != 'Windows'
        shell: bash
        env:
          PLATFORM: ${{ matrix.platform }}
          ENVIRONMENT: ${{ matrix.environment }}
        run: |
          bundle="camera-toolbox-${PLATFORM}-${ENVIRONMENT}"
          mkdir -p "dist/${bundle}"
          install -m 755 "target/${PLATFORM}/release/camera-toolbox" "dist/${bundle}/"
          install -m 644 README.md "dist/${bundle}/"
          tar -C dist -czf "dist/${bundle}.tar.gz" "${bundle}"

      - name: Package Windows release
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          PLATFORM: ${{ matrix.platform }}
          ENVIRONMENT: ${{ matrix.environment }}
        run: |
          $bundle = "camera-toolbox-$env:PLATFORM-$env:ENVIRONMENT"
          New-Item -ItemType Directory -Force -Path "dist/$bundle" | Out-Null
          Copy-Item "target/$env:PLATFORM/release/camera-toolbox.exe" "dist/$bundle/"
          Copy-Item README.md "dist/$bundle/"
          Compress-Archive -Path "dist/$bundle" -DestinationPath "dist/$bundle.zip" -Force

      - name: Upload release archive
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.platform }}-${{ matrix.environment }}
          path: dist/camera-toolbox-${{ matrix.platform }}-${{ matrix.environment }}.*
          if-no-files-found: error

  build-linux:
    name: Build ${{ matrix.platform }} ${{ matrix.environment }}
    runs-on: ${{ matrix.runner }}
    container:
      image: ubuntu:${{ matrix.ubuntu_version }}

    strategy:
      fail-fast: false
      matrix:
        platform:
          - local
          - cv610
          - ssh
        environment:
          - linux-x86_64-ubuntu20
          - linux-x86_64-ubuntu22
          - linux-aarch64-ubuntu20
          - linux-aarch64-ubuntu22
        include:
          - environment: linux-x86_64-ubuntu20
            runner: ubuntu-22.04
            ubuntu_version: "20.04"
          - environment: linux-x86_64-ubuntu22
            runner: ubuntu-22.04
            ubuntu_version: "22.04"
          - environment: linux-aarch64-ubuntu20
            runner: ubuntu-22.04-arm
            ubuntu_version: "20.04"
          - environment: linux-aarch64-ubuntu22
            runner: ubuntu-22.04-arm
            ubuntu_version: "22.04"

    steps:
      - name: Install Linux build dependencies
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            build-essential \
            ca-certificates \
            curl \
            git \
            pkg-config \
            libasound2-dev \
            libegl1-mesa-dev \
            libgl1-mesa-dev \
            libgtk-3-dev \
            libwayland-dev \
            libx11-dev \
            libxkbcommon-dev
      - name: Check out source
        uses: actions/checkout@v4
      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Build selected release
        run: ./build.sh "${{ matrix.platform }}" release
      - name: Package Linux release
        env:
          PLATFORM: ${{ matrix.platform }}
          ENVIRONMENT: ${{ matrix.environment }}
        run: |
          bundle="camera-toolbox-${PLATFORM}-${ENVIRONMENT}"
          mkdir -p "dist/${bundle}"
          install -m 755 "target/${PLATFORM}/release/camera-toolbox" "dist/${bundle}/"
          install -m 644 README.md "dist/${bundle}/"
          tar -C dist -czf "dist/${bundle}.tar.gz" "${bundle}"
      - name: Upload release archive
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.platform }}-${{ matrix.environment }}
          path: dist/camera-toolbox-${{ matrix.platform }}-${{ matrix.environment }}.tar.gz
          if-no-files-found: error

  publish:
    name: Publish GitHub Release
    needs:
      - build-native
      - build-linux
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Download release archives
        uses: actions/download-artifact@v4
        with:
          pattern: release-*
          path: dist
          merge-multiple: true
      - name: Verify release archives
        run: |
          for platform in local cv610 ssh; do
            test -f "dist/camera-toolbox-${platform}-macos-aarch64.tar.gz"
            test -f "dist/camera-toolbox-${platform}-windows-x86_64.zip"
            test -f "dist/camera-toolbox-${platform}-linux-x86_64-ubuntu20.tar.gz"
            test -f "dist/camera-toolbox-${platform}-linux-x86_64-ubuntu22.tar.gz"
            test -f "dist/camera-toolbox-${platform}-linux-aarch64-ubuntu20.tar.gz"
            test -f "dist/camera-toolbox-${platform}-linux-aarch64-ubuntu22.tar.gz"
          done
      - name: Create or update GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          target_commitish: ${{ github.sha }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: |
            dist/camera-toolbox-*.tar.gz
            dist/camera-toolbox-*.zip
